ext.keystore = [
        keystore     : '/key/generator',
        keystorePwd  : 'generator12345',
        keystoreAlias: 'generator'
]

ext.keystoreDebug = [
        keystore     : '/key/generator-debug',
        keystorePwd  : 'generator12345d',
        keystoreAlias: 'generator-debug'
]

ext.keystore.signingConfigs = {
    storeFile file(new File(getRootDir(), keystore.keystore))
    storePassword keystore.keystorePwd
    keyAlias keystore.keystoreAlias
    keyPassword keystore.keystorePwd
}

ext.keystore.debugSigningConfigs = {
    storeFile file(new File(getRootDir(), keystoreDebug.keystore))
    storePassword keystoreDebug.keystorePwd
    keyAlias keystoreDebug.keystoreAlias
    keyPassword keystoreDebug.keystorePwd
}
